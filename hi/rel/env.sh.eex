#!/bin/sh
export PUBLIC_HOSTNAME=`curl -s ${ECS_CONTAINER_METADATA_URI}/task | jq -r ".Containers[0].Networks[0].IPv4Addresses[0]"`
export RELEASE_DISTRIBUTION=name
export PUBLIC_HOSTNAME="${PUBLIC_HOSTNAME:-127.0.0.1}"
NODE_NAME=${NODE_NAME:-hi_node}
export AWS_RELEASE_NODE=${NODE_NAME}@${PUBLIC_HOSTNAME}
export RELEASE_NODE="${RELEASE_NODE:-AWS_RELEASE_NODE}"
